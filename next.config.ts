// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'logos-world.net'], // Remove https://
  },
};

module.exports = nextConfig;
